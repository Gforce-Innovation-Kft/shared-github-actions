{
  "name": "gforce-gha-src",
  "version": "0.1.0",
  "private": true,
  "description": "Single source of truth: all TypeScript implementation behind the Gforce GitHub Actions",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --coverage --config jest.config.cjs",
    "lint": "eslint ."
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@octokit/rest": "^19.0.13"
  }
}
