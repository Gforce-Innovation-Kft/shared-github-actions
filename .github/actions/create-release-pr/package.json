{
  "name": "@gforce/create-release-pr",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action: create or update a release pull request between two branches.",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --coverage --config jest.config.js",
    "bundle": "ncc build src/index.ts -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@gforce/core": "*",
    "@gforce/github-actions-runtime": "*"
  }
}
