{
  "name": "@gforce/actions-manifests",
  "version": "0.1.0",
  "private": true,
  "description": "Shared config + build orchestration for the bundled TypeScript action entries",
  "scripts": {
    "build:all": "npm run build --prefix github-branch-sync && npm run build --prefix github-release-pr-create && npm run build --prefix sf-apex-test-select"
  }
}
